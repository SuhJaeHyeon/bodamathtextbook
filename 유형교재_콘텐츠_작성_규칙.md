# 유형교재 콘텐츠 작성 규칙

> 기준 파일: `유형교재_guide_design.html`  
> 이 문서는 Claude가 유형교재 HTML을 제작할 때 따르는 콘텐츠 작성 규칙이다.  
> 구조·CSS 규칙은 기준 파일 내 주석을 참조한다.

---

## 핵심 원칙 — 페이지당 유형 1개

**페이지 1개 = 유형 1개**가 제1의 규칙이다. 하나의 페이지에는 반드시 하나의 유형만 담는다.

각 페이지의 구성:
- **유형 설명** 1개 (유형명 + 개념 요약)
- **대표문제** 1개
- **추가 문제** 3~4개 (최대 4개)

> col-left에는 유형 설명(type-group)과 대표문제(prob-rep)가 이미 들어가므로, 추가 문제는 col-left 높이에 따라 1~2개만 들어가고 나머지는 col-right으로 넘어간다. 배분은 JS(`redistributeProbs`)가 자동 처리한다.

---

## 0. HTML 파일 저장 경로 및 파일명 규칙

### 저장 폴더
기존 개념교재 HTML과 **동일한 폴더**에 저장한다.

```
교재_html/
├── 중1-1/   ← 중1-1 유형교재 HTML도 여기에 저장
├── 중1-2/
├── 중2-1/
├── 중2-2/
└── 중3-1/
```

### 파일명 규칙
대응하는 **기존 개념교재 파일명 앞에 `[유형] `을 붙인다.**

| 기존 개념교재 파일명 | 유형교재 파일명 |
|---|---|
| `중학 1-1_I-1단원_01 소인수분해.html` | `[유형] 중학 1-1_I-1단원_01 소인수분해.html` |
| `중학 2-1_Ⅲ-1단원_01 부등식과 그 성질.html` | `[유형] 중학 2-1_Ⅲ-1단원_01 부등식과 그 성질.html` |

### 대응 관계 파악 방법
`curriculum_mapping.md`의 소단원 목록을 기준으로, 작업 대상 소단원명이 일치하는 기존 파일을 찾는다.  
일치하는 기존 파일이 없는 경우 사용자에게 확인 후 진행한다.

---

## 0-1. HTML 작업 시 콘텐츠 소스 및 추출 방식

### 유형별 HTML 콘텐츠 구성
모든 콘텐츠는 **문제은행 PDF 단일 소스**에서 추출한다.

**문제은행 PDF (유형 설명 + 문제 소스)**
- 출처: 사용자가 소단원/단원 기준으로 직접 제공
- 추출 항목: 유형명 + 유형 설명 + 문제 내용 + 선지 + 난이도
- 문제 번호는 무시 (파일마다 임시로 붙은 번호)
- 문제은행은 유형별 5문제씩 순서대로 구성되어 있음 (대체로)

### 유형 설명 재구성 규칙
문제은행 PDF의 유형 설명을 **그대로 옮기지 않고** 아래 기준으로 재구성한다.

- **대상 독자**: 중학생도 이해할 수 있도록 쉬운 표현 사용
- **핵심 요지 유지**: 원문과 동일한 핵심 내용을 다루되, 보다 간결하게 압축
- **원문 참고 용도**: 문제은행의 유형 설명은 재구성의 토대이지 복사 대상이 아님

#### 재구성 판단 기준

아래 중 하나라도 해당되면 **재구성 실패**로 간주하고 다시 작성한다.

- PDF 원문의 문장을 그대로 쓴 구간이 한 문장 이상 있다.
- PDF의 번호 목록(①②③ 또는 가·나·다 등) 구조를 그대로 복사했다.
- 교과서 정의 문체("~는 ~이다" 나열 형식)를 그대로 사용했다.

#### Before / After 예시

**Before (재구성 실패 — PDF 원문 복사)**
> 소수: 1보다 큰 자연수 중에서 1과 자기 자신만을 약수로 가지는 수  
> 합성수: 1보다 큰 자연수 중에서 소수가 아닌 수  
> 1은 소수도 합성수도 아니다.

**After (재구성 완료 — 학생 친화적 설명)**
> 약수가 1과 자기 자신뿐인 수를 **소수**, 약수가 3개 이상인 수를 **합성수**라고 한다. 1은 약수가 1개뿐이라 소수도 합성수도 아니다. 소수 중 가장 작은 수는 2이고, 짝수 소수는 2뿐이다.

---

**Before (재구성 실패 — PDF 번호 목록 구조 복사)**
> ① 1은 소수도 아니고 합성수도 아니다.  
> ② 2는 소수 중 가장 작은 수이며 유일한 짝수 소수이다.  
> ③ 소수의 약수는 2개이다.

**After (재구성 완료 — 자연스러운 문장으로 통합)**
> 소수의 약수는 항상 2개뿐이다. 소수 중 가장 작은 수이자 유일한 짝수 소수는 2이다. 1은 약수가 1개뿐이어서 소수·합성수 어디에도 속하지 않는다.

#### 작성 후 셀프체크

유형 설명을 작성한 뒤, HTML에 넣기 전에 아래를 확인한다.

- [ ] PDF 원문과 문장 단위로 비교했을 때 그대로 가져온 문장이 없는가?
- [ ] 번호 목록(①②③, 가·나·다) 구조를 그대로 쓰지 않았는가?
- [ ] 중학생이 처음 보더라도 이해할 수 있는 표현인가?

### 문제은행 처리 순서
1. 문제은행 PDF 렌더링
2. 유형명 추출 → `curriculum_mapping.md`의 유형명과 대조해서 순서 검증
3. 5개씩 묶어서 유형별 그룹화
4. 유형 설명 + 문제 내용 + 선지 + 난이도 저장
5. **작업 전 정답표 작성** (아래 참조)
6. HTML 생성

### 작업 전 정답표 작성

HTML 작성 전, 아래 형식의 정답표를 먼저 완성한다. 난이도 정렬 → 번호 부여 → 정답 기록을 한 번에 처리해두면 HTML 작성 중 역추적이 불필요하고, 빠른정답 페이지도 이 표에서 바로 꺼내 쓴다.

| 유형 | 구분 | 문제번호 | 난이도 | 정답 |
|---|---|---|---|---|
| 유형01 | 대표문제 | 001 | — | ③ |
| 유형01 | 문제1 | 002 | dot1 | ② |
| 유형01 | 문제2 | 003 | dot2 | ④ |
| 유형01 | 문제3 | 004 | dot2 | ① |
| 유형01 | 문제4 | 005 | dot3 | ⑤ |
| 유형02 | 대표문제 | 006 | — | … |
| … | … | … | … | … |

- 대표문제는 난이도 표시 없이 유형의 첫 번째 번호를 차지한다.
- 추가 문제는 난이도 오름차순(dot1→dot2→dot3)으로 정렬한 후 번호를 부여한다.
- 같은 난이도 내 순서는 문제은행 PDF의 원래 순서를 유지한다.

---

## 1. 고정값 / 가변값

고정값과 가변값의 전체 목록 및 연동 규칙은 **`유형교재_guide_design.html` 상단 주석**에 정의되어 있다. 반드시 그 주석을 우선 확인할 것.

핵심 연동 규칙 요약:
- `ans-hd__sec-num` = `unit-badge` (동일값)
- `page-footer__topic` = `ans-hd__sec-desc` = `page-hd__title` (동일값)

---

## 2. 문제번호 규칙

- 3자리 숫자로 표기한다. 예) `001`, `006`
- 유형01의 첫 문제가 `001`이고, 이후 유형에서 **번호를 이어간다.**
  - 유형01: 001~005 → 유형02: 006~009 → 유형03: 010~...
- `prob-rep`(대표문제)도 번호를 하나 차지한다.
- 번호는 각 자릿수를 `<span>` 태그로 분리하여 색상 규칙을 적용한다.
  - `0` → `<span class="n0">0</span>` (#C1C1C1, 부모 색상 상속)
  - 비-0 숫자 → `<span class="nx">숫자</span>` (#2FA5A5)
  - 예) 007 → `<span class="n0">0</span><span class="n0">0</span><span class="nx">7</span>`

---

## 3. 페이지번호 규칙

- `page-footer__num`은 3자리 숫자로 표기한다. 예) `001`, `002`
- 첫 페이지(page1)가 `001`이고, 이후 페이지에서 순서대로 증가한다.
- 빠른정답 페이지(page-ans)도 번호를 하나 차지한다.
  - 예) page1=001 / page2=002 / page-ans=003

---

## 4. 난이도(dot) 부여 규칙

- `dot.on` 개수로 난이도를 표현한다. 1개(하) / 2개(중) / 3개(상)
- 항상 `.dot` 3개를 마크업하고, 켜진 것만 `.on` 클래스를 추가한다.

```html
<!-- 난이도 2 (중) 예시 -->
<div class="diff-dots">
  <span class="dot on"></span>
  <span class="dot on"></span>
  <span class="dot"></span>
</div>
```

### 난이도 판별 기준

문제은행 PDF에 난이도가 표기되어 있는 경우 아래 매핑을 따른다.

| PDF 표기 | dot 개수 |
|---|---|
| 하 | dot1 |
| 중하, 중 | dot2 |
| 상, 최상 | dot3 |

난이도가 표기되어 있지 않은 파일의 경우, 각 유형별로 아래 순서를 따른다.

| 순서 | 구분 | 난이도 |
|---|---|---|
| 첫 번째 문제 | 대표문제 | 표시 없음 |
| 두 번째, 세 번째 문제 | 추가 문제 | dot2 |
| 네 번째, 다섯 번째 문제 | 추가 문제 | dot3 |

위 기준을 적용하기 어려운 예외적인 경우에만 사용자에게 확인한다.

### 유형 내 문제 순서

한 유형 안에서 문제는 **난이도 오름차순**으로 배치한다. (dot1 → dot2 → dot3)

---

## 5. 선지 정렬 규칙

`prob__choices-*` 및 `prob-rep__choices-*` 변형 클래스 중 하나를 선택한다.  
판단 기준은 **가장 긴 선지의 길이** (KaTeX 수식은 소스 문자 수보다 넓게 렌더링됨에 유의).

| 클래스 접미사 | 사용 조건 |
|---|---|
| `-wrap` | 선지 전체가 한 줄에 들어오는 경우 |
| `-3col` | 가장 긴 선지 **3~6자** |
| `-2col` | 가장 긴 선지 **7~9자** |
| `-stack` | 가장 긴 선지 **9자 이상**, 또는 1열이 자연스러울 때 |

> 유형교재 col 가용 너비: **약 251.5px**  
> 선지에 KaTeX 수식이 포함된 경우 `-wrap` 판단 시 실제 렌더링 너비를 기준으로 한다.

모든 선지 컨테이너에 `padding-right: 4px`가 CSS에 전역 적용되어 있다.

---

## 6. prob__space (풀이 여백) 규칙

- 기본값: `60px`
- HTML 작성 시 항상 `<div class="prob__space"></div>`만 사용한다. (`style` 속성 없이)
- **Claude가 임의로 값을 조정하지 않는다.** 값 변경이 필요하면 사용자가 직접 지정한다.

```html
<!-- 항상 이 형태로만 작성 -->
<div class="prob__space"></div>

<!-- 사용자가 명시적으로 지정한 경우에만 아래 형태 허용 -->
<div class="prob__space" style="--space: 90px"></div>
```

---

## 7. badge--imp (중요 배지) 사용 규칙

### 적용 기준
`curriculum_mapping.md`에서 해당 유형에 **⭐ 표시**가 있으면 `badge--imp`를 추가한다.

```
| 01. 소인수분해 | 유형 04 ⭐ | 소인수분해 | 0053 |   ← badge--imp 추가
| 01. 소인수분해 | 유형 05   | 소인수 구하기 | 0057 | ← 추가 안 함
```

### 마크업 위치
- **`type-group` 내 첫 번째 자식**으로 배치한다.
- `type-group`이 `position: relative`를 가지므로 배지가 자동으로 type-group 기준으로 위치한다.
- 페이지 레벨(`.page`의 직접 자식)에 두지 않는다.

```html
<div class="type-group">
  <div class="badge--imp">
    <div class="badge--imp__inner">
      <div class="badge--imp__pill">중요</div>
      <div class="badge--imp__tri"></div>
    </div>
  </div>
  <div class="type-label-row">...</div>
  ...
</div>
```

---

## 8. 빠른정답 페이지 작성 규칙

- 모든 정답은 `ans-col--left` 단일 컬럼에 넣는다. `ans-col--right`는 비워둔다.
- `ans-card__row` 1개에 `ans-item` 2개를 배치한다 (좌·우 각 1문제).
- 문제 수가 홀수이면 마지막 `ans-card__row`의 오른쪽 `ans-item`은 빈 div로 둔다.
  ```html
  <div class="ans-item"></div>
  ```
- `ans-item__ans` 형식:
  - 객관식: 번호 기호 그대로. 예) `②`, `④`
  - 단답형: 숫자 또는 식 그대로. 예) `5`, `-3`
  - 서술형: `풀이 참조`
- `ans-hd__sec-num`은 `unit-badge`와 동일한 단원번호를 입력한다.
- `ans-hd__sec-desc`는 `page-hd__title`과 동일한 소단원명을 입력한다.

---

## 9. 기술 스택 — KaTeX · 로고 · 폰트

### KaTeX
```html
<!-- <head> 안에 CSS 참조 -->
<link rel="stylesheet" href="./katex/katex.min.css">

<!-- </body> 직전에 JS 참조 (defer) -->
<script defer src="./katex/katex.min.js"></script>
<script defer src="./katex/contrib/auto-render.min.js"
  onload="renderMathInElement(document.body, {
    delimiters: [
      { left: '$$', right: '$$', display: true },
      { left: '$',  right: '$',  display: false }
    ]
  }); redistributeProbs();"></script>
```

- KaTeX 파일 위치: `교재_html/<학년>/katex/` (각 학년 폴더 안의 사본 사용)
- 경로는 HTML과 같은 폴더에 `katex/`가 있으므로 `./katex/`로 참조
- `onload` 안에서 KaTeX 렌더 완료 후 `redistributeProbs()`를 반드시 호출

### 로고
- 로고 파일: `리브랜딩/bodamath_logo.png`
- 유형교재 HTML은 `<img>` 태그로 삽입한다. HTML 저장 위치(`교재_html/<학년>/`)에서 두 단계 상위이므로 상대경로는 `../../bodamath_logo.png`.

```html
<img src="../../bodamath_logo.png" alt="보다수학" class="page-footer__logo">
```

> 개념교재 HTML의 base64 방식과 다름. 유형교재는 반드시 `<img>` 태그 + 상대경로 방식 사용.

### 폰트
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css">
```

---

## 10. 페이지 구성 패턴

소단원 1개는 아래 순서로 페이지를 구성한다.

| 순서 | 클래스 | id | 설명 |
|---|---|---|---|
| 1 | `page` | `page1` | 첫 페이지. 대형 헤더(`page-hd`) + `unit-badge`. gap 6px |
| 2~N-1 | `page page--cont` | `page2`, `page3`… | 이후 페이지. 소형 헤더(`page-hd--sm`). gap 12px |
| (해당 시) | `page page--cont page--adv` | — | **유형 UP 페이지** (아래 참조) |
| 마지막 | `page page--ans` | `page-ans` | 빠른정답 페이지 |

### 유형 UP 페이지 (`page--adv`)

**적용 기준**: `curriculum_mapping.md`에서 해당 유형에 **UP 표시**가 있으면 `page--adv`를 추가한다.

```
| 01. 소인수분해 | 유형 UP 09 ⭐ | 제곱인 수 만들기 | 0073 |  ← page--adv 추가
| 01. 소인수분해 | 유형 05      | 소인수 구하기   | 0057 |  ← 추가 안 함
```

- `page--cont`와 **구조가 완전히 동일**하고, CSS 색상만 `#2FA5A5 → #FF4043`으로 변경된다.
- 클래스를 두 개 함께 쓴다: `class="page page--cont page--adv"`
- UP + ⭐가 함께 표기된 유형은 `page--adv`와 `badge--imp`를 **둘 다** 적용한다.

### 페이지당 문제 배치 원칙
- `col-left`: type-group(유형 설명) + prob-rep(대표문제) + 추가 문제 중 높이가 허용하는 만큼 (보통 1~2개)
- `col-right`: col-left 높이를 초과한 나머지 추가 문제 — JS(`redistributeProbs`)가 자동 이동
- HTML 작성 시 모든 추가 문제(prob)를 col-left에 순서대로 쓰면 된다. col-right는 비워둔다.

---

## 11. redistributeProbs() — 자동 배분 JS

`유형교재_guide_design.html` 하단의 `<script>` 블록을 **실제 제작 파일에도 그대로 복사**한다.

```
동작 원리:
1. KaTeX 렌더 완료 후 redistributeProbs() 호출
2. col-left가 높이를 벗어나면
   마지막 prob을 col-right 맨 위로 이동 (반복)
```

- **HTML 작성 시 col-right에 prob을 수동으로 넣지 않는다.** JS가 처리한다.
- col-right는 항상 빈 div로 선언해 둔다.
- JS를 포함하지 않으면 col-right가 비어 내용이 잘릴 수 있다.

---

## 12. prob-boogi (조건·보기 박스) 규칙

문제 안에 별도 박스로 묶어야 하는 내용(조건, 보기, 〈조건〉 등)에 사용한다.

### 적용 대상
- **〈조건〉 박스**: "가, 나, 다" 등 조건이 나열된 경우
- **〈보기〉 박스**: ㄱ, ㄴ, ㄷ 등 보기가 나열된 경우
- **번호 목록**: 문제 본문이 아닌, 별도 번호 리스트(①②③ 등)가 박스 안에 제시된 경우

### 마크업 방법

```html
<!-- 조건/보기 박스 -->
<div class="prob-boogi">
  가. 내용1<br>
  나. 내용2
</div>

<!-- 수식 포함 시 KaTeX 그대로 사용 -->
<div class="prob-boogi">
  ㄱ. $a$는 소수이다.<br>
  ㄴ. $b$는 합성수이다.
</div>
```

### 위치
- `prob__body` 또는 `prob-rep__body` 내에서 `prob__q`(문제 텍스트) 바로 아래에 배치한다.
- `prob__space` 앞에 위치한다.

---

## 13. prob-figure (그림 플레이스홀더) 규칙

문제에 그림·도형·그래프가 포함되는 경우, 해당 위치에 플레이스홀더를 삽입한다.

### 적용 기준
- PDF 문제에 그림, 도형, 그래프가 있는 경우 **반드시** 플레이스홀더를 추가한다.
- Claude가 그림을 생략하거나 텍스트로 대체하지 않는다.

### 마크업 방법

```html
<!-- 도형이 있는 경우 -->
<div class="prob-figure">(도형)</div>

<!-- 그래프가 있는 경우 -->
<div class="prob-figure">(그래프)</div>

<!-- 그림이 있는 경우 -->
<div class="prob-figure">(그림)</div>
```

### 텍스트 기준
| PDF 내용 | 플레이스홀더 텍스트 |
|---|---|
| 도형 (삼각형, 원, 사각형 등) | `(도형)` |
| 그래프 (좌표, 함수 등) | `(그래프)` |
| 그 밖의 그림 | `(그림)` |

### 위치
- `prob__body` 또는 `prob-rep__body` 내에서 관련 문제 텍스트 직후에 배치한다.
- `prob__space` 앞에 위치한다.
